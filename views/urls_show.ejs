<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("./partials/_head") %>
  <title>URL - powTinyApp</title>
</head>

<body>
  <%- include("./partials/_header") %>
  <div class="card text-center">
      <div class="card-header"></div>
      <div class="card-body">
        <h5 class="card-title">powTinyURL for: <%= longURL %></h5>
        <p class="card-text" style="margin-bottom: 12px;">Short URL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></p>
        <p class="card-text">Created Date: <%= createdDate %></p>
      </div>
    <div class="card-footer text-muted">
      <form class="form-inline justify-content-center" action="/urls/<%= shortURL %>?_method=PUT" method="POST">
        <label for="longURL">Update URL</label>
        <input class="form-control" type="text" name="longURL" placeholder="http://www." style="width: 300px; margin: 1em" required>
        <button class="btn btn-primary">Update</button>
      </form>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <%- include("./partials/_script") %>
</body>
</html>